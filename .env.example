# Application
APP_NAME=AI Odoo Finder
ENVIRONMENT=development

# Backend
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database (Neon - non-secret endpoint example)
# For production, prefer NEON_DATABASE_URL and keep DATABASE_URL for local dev
DATABASE_URL=sqlite:///./data/ai_odoofinder.db
NEON_HOST=your-neon-project-region.neon.tech
NEON_DATABASE=your_db
NEON_USER=your_user
# Use NEON_PASSWORD only in local .env (leave empty in example)
NEON_PASSWORD=
# Use NEON_DATABASE_URL only in local .env (leave empty in example)
NEON_DATABASE_URL=

# OpenRouter (leave key empty in example)
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=

# Render (leave key empty in example)
RENDER_API_BASE_URL=https://api.render.com/v1
RENDER_API_KEY=
RENDER_SERVICE_ID=

# GitHub (optional, leave empty in example)
GITHUB_TOKEN=
